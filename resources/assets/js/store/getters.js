export const fields = (state) => {
    return state.fields;
}

export const doNotValidateFields = (state) => {
    return state.doNotValidateFields;
}

export const bmi = (state) => {
    return state.bmi;
}

export const rating = (state) => {
    return state.rating;
}